<!DOCTYPE html>
<html lang="en">
<%- include("./partials/head.ejs") %>
<body>
  <%- include("./partials/navbar.ejs") %>

 <div>
    <div>
        <% if (blogs.length > 0) { %>
         <% blogs.map((blog)=>{ %>
         <h1><%= blog.title %></h1>
       <% }) }%>
    </div>
 </div>
</body>
</html>